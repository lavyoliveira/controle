import React from 'react';
import { View } from 'react-native';
import Header from '../widgets/header';

const Home = () => {
    return (
        <View>
            <Header/>
        </View>
    );
};

export default Home;
